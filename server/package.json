{
  "name": "test-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",

  "scripts": {
    "start": "node index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json"
  },  
  "dependencies": {    
    "tsc-alias": "^1.8.16"
  }
}
